package com.willis.heimdall

class EnvironmentController {

    def scaffold = true
	
	def calendar = {
		// TODO : sort out the implementation below, and add some error handling for null objects
		//def env = Environment.findByEnvironmentId(params.id)
		//[ environment : env ]
	}
}
